# Phase 1.3.2: Refactor Landing Page UI and Migrate Legacy Content - Patterns

## 1. Files to Create or Modify

### 1.1 Locale Files (Data Source)
- `apps/web/src/locales/en.json` (modified)
- `apps/web/src/locales/id.json` (modified)
  - **Role**: Data Source for all UI components.
  - **Data Flow**: Legacy YAML data -> JSON files -> React components via `react-i18next`.

### 1.2 Landing Page Components (UI Structure)
The following files are to be created or updated within `apps/web/src/components/landing/`:
- `HeroSection.tsx` (or update `Hero.tsx`)
- `AboutUsSection.tsx` (or update `About.tsx`)
- `WhyAttentiveSection.tsx` (or update `WhyUs.tsx`)
- `PsychologistsSection.tsx` (or update `Psychologists.tsx`)
- `TestimonialsSection.tsx` (or update `Testimonials.tsx` / `Testimonials` in `LandingPage.tsx`)
- `ServicesSection.tsx` (or update `Products.tsx`/`Services.tsx`)
- `PartnerContentSection.tsx` (or update `Insights.tsx`)
- `CTASection.tsx` (or update `CtaScreening.tsx`)
- `ClinicsLocationsSection.tsx` (or update `Contact.tsx`)
  - **Role**: Presentational Components.
  - **Data Flow**: Receive translation keys and render mapped data based on Figma constraints. Will use Framer Motion for scroll-driven animations and Shadcn UI.

### 1.3 Page Orchestrator
- `apps/web/src/pages/LandingPage.tsx` (modified)
  - **Role**: Orchestrator Component.
  - **Data Flow**: Imports and strictly stacks the 9 components vertically as required by the spec.

### 1.4 Assets
- `apps/web/public/images/*` (created/migrated)
  - **Role**: Static assets.
  - **Data Flow**: Loaded via absolute paths (e.g. `/images/...`) from the UI components.

---

## 2. Closest Existing Analogs & Code Excerpts

### Analog for Components: `Hero.tsx`
The current `Hero.tsx` provides a strong baseline pattern for:
- Importing `useTranslation`
- Utilizing Framer Motion for reveal animations
- Structuring Shadcn components with Tailwind classes

**Excerpt (`apps/web/src/components/landing/Hero.tsx`):**
```tsx
import { useTranslation } from "react-i18next";
import { motion } from "framer-motion";
import { Button } from "@/components/ui/button";

export function Hero() {
  const { t } = useTranslation();

  return (
    <section id="hero" className="relative h-screen w-full flex items-center justify-center overflow-hidden bg-dominant">
      <div className="container mx-auto px-4 text-center z-10">
        <motion.h1 
          className="text-5xl md:text-7xl font-bold tracking-tight mb-4 font-sans text-secondary" 
          initial={{ opacity: 0, y: 20 }} 
          whileInView={{ opacity: 1, y: 0 }} 
          transition={{ type: "spring", stiffness: 100, damping: 20, delay: 0.2 }} 
          viewport={{ once: true }}
        >
          {t("hero.weAre")} <span className="text-primary">{t("hero.brand")}</span>
        </motion.h1>
        {/* ... */}
      </div>
    </section>
  );
}
```

### Analog for Orchestrator: `LandingPage.tsx`
The current `LandingPage.tsx` serves as the orchestrator analog. It demonstrates how sections are imported and stacked within a `flex flex-col` container.

**Excerpt (`apps/web/src/pages/LandingPage.tsx`):**
```tsx
import React from 'react';
import { Navbar } from '../components/common/Navbar';
import { Hero, WhyUs, Products, Psychologists, Insights, CtaScreening, Contact, Footer } from '../components/landing';

const Testimonials = () => <section>Testimonials</section>;

export const LandingPage = () => {
  return (
    <div className="flex flex-col min-h-screen">
      <Navbar />
      <Hero />
      <WhyUs />
      {/* Must update this sequence to strictly match the 9 specified components */}
      <Footer />
    </div>
  );
};
```
