---
wave: 1
depends_on: ["Phase 1.3"]
files_modified:
  - "apps/web/public/images/**"
  - "apps/web/src/locales/en.json"
  - "apps/web/src/locales/id.json"
  - "apps/web/components/ui/button.tsx"
  - "apps/web/components/ui/card.tsx"
  - "apps/web/components/ui/avatar.tsx"
  - "apps/web/src/components/landing/HeroSection.tsx"
  - "apps/web/src/components/landing/AboutUsSection.tsx"
  - "apps/web/src/components/landing/WhyAttentiveSection.tsx"
  - "apps/web/src/components/landing/PsychologistsSection.tsx"
  - "apps/web/src/components/landing/TestimonialsSection.tsx"
  - "apps/web/src/components/landing/ServicesSection.tsx"
  - "apps/web/src/components/landing/PartnerContentSection.tsx"
  - "apps/web/src/components/landing/CTASection.tsx"
  - "apps/web/src/components/landing/ClinicsLocationsSection.tsx"
  - "apps/web/src/pages/LandingPage.tsx"
autonomous: true
---

# Phase 01.3.2 Execution Plan
**Goal:** Refactor Landing Page UI to new Figma design and migrate legacy YAML content.

## requirements
- null

## must_haves
- All 9 components must be vertically stacked in the exact order specified.
- Shadcn UI (radix/lucide-react) components used where appropriate.
- i18n JSON files correctly populated from the legacy YAML.
- "Take the First Step" CTA styled with `#2563EB`.

## Wave 1: Data Migration & Setup

<task>
  <read_first>
    - /Users/kytama/Projects/Neo-Attentive/attentiveid.github.io/data/en/homepage.yml
    - apps/web/src/locales/en.json
    - apps/web/src/locales/id.json
  </read_first>
  <action>
    Create the translation files at `apps/web/src/locales/en.json` and `apps/web/src/locales/id.json`. Write a script to convert `/Users/kytama/Projects/Neo-Attentive/attentiveid.github.io/data/en/homepage.yml` into JSON format and save it to both files, ensuring the following top-level keys exist: "hero", "about_us", "why_attentive", "psychologists", "testimonials", "services", "partner_content", "cta", "clinics_locations". For "partner_content", set `{"title": "Coming soon", "description": "We are currently preparing this section. Check back later!"}`. Verify both files are valid JSON.
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/locales/en.json | grep '"hero"'` returns a match.
    - `cat apps/web/src/locales/id.json | grep '"cta"'` returns a match.
    - `cat apps/web/src/locales/en.json | grep '"Coming soon"'` returns a match.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - package.json
  </read_first>
  <action>
    Install required shadcn UI components into the React web app. From the `apps/web` directory, execute exactly: `npx -y shadcn@latest add button card avatar -y` to add the Button, Card, and Avatar components. Ensure they are placed in `apps/web/components/ui/`.
  </action>
  <acceptance_criteria>
    - `ls apps/web/components/ui/button.tsx` exits with 0.
    - `ls apps/web/components/ui/card.tsx` exits with 0.
    - `ls apps/web/components/ui/avatar.tsx` exits with 0.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - .planning/phases/01.3.2-refactor-landing-page-ui-and-migrate-legacy-content/01.3.2-CONTEXT.md
  </read_first>
  <action>
    Copy the entire legacy images directory from `/Users/kytama/Projects/Neo-Attentive/attentiveid.github.io/images/` to `apps/web/public/images/`. Run `mkdir -p apps/web/public/images` then `cp -R /Users/kytama/Projects/Neo-Attentive/attentiveid.github.io/images/* apps/web/public/images/`. Ignore any copying errors if files don't exist.
  </action>
  <acceptance_criteria>
    - `ls apps/web/public/images/` exits with 0.
  </acceptance_criteria>
</task>

## Wave 2: Scaffold Section Components

<task>
  <read_first>
    - .planning/phases/01.3.2-refactor-landing-page-ui-and-migrate-legacy-content/01.3.2-UI-SPEC.md
  </read_first>
  <action>
    Create the following files in `apps/web/src/components/landing/`: `HeroSection.tsx`, `AboutUsSection.tsx`, `WhyAttentiveSection.tsx`, `PsychologistsSection.tsx`, `TestimonialsSection.tsx`, `ServicesSection.tsx`, `PartnerContentSection.tsx`, `CTASection.tsx`, and `ClinicsLocationsSection.tsx`.
    Each file must export a functional React component returning a `<section>` with an `id` matching its name (e.g., `<section id="hero-section">`). Import `useTranslation` from `react-i18next`. Do not import any shadcn components yet.
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/components/landing/HeroSection.tsx | grep 'export function HeroSection'` returns a match.
    - `cat apps/web/src/components/landing/ClinicsLocationsSection.tsx | grep 'id="clinics-locations-section"'` returns a match.
    - `ls apps/web/src/components/landing/TestimonialsSection.tsx` exits with 0.
  </acceptance_criteria>
</task>

## Wave 3: UI Integration and Styling

<task>
  <read_first>
    - apps/web/src/components/landing/HeroSection.tsx
    - apps/web/src/components/landing/AboutUsSection.tsx
    - apps/web/src/components/landing/WhyAttentiveSection.tsx
  </read_first>
  <action>
    Update `HeroSection.tsx`, `AboutUsSection.tsx`, and `WhyAttentiveSection.tsx` to use the i18n data. In `HeroSection.tsx`, import the Button from `@/components/ui/button` (or relative path if needed) and use it with text "Take the First Step" and Tailwind class `bg-[#2563EB]`. Set the main background of sections to `bg-[#FFFFFF]` and use `bg-[#F8FAFC]` for inner sections. Apply `text-[48px] font-bold` for Hero title, `text-[24px] font-bold` for section titles.
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/components/landing/HeroSection.tsx | grep 'Take the First Step'` returns a match.
    - `cat apps/web/src/components/landing/HeroSection.tsx | grep 'bg-\[#2563EB\]'` returns a match.
    - `cat apps/web/src/components/landing/AboutUsSection.tsx | grep 'bg-\[#FFFFFF\]'` returns a match.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - apps/web/src/components/landing/PsychologistsSection.tsx
    - apps/web/src/components/landing/TestimonialsSection.tsx
    - apps/web/src/components/landing/ServicesSection.tsx
    - apps/web/src/components/landing/PartnerContentSection.tsx
  </read_first>
  <action>
    Update the next 4 sections to map over their respective i18n arrays (from `useTranslation`) and use the `Card` component from `@/components/ui/card`. For `PartnerContentSection.tsx`, explicitly render the fallback copy: "Coming soon" and "We are currently preparing this section. Check back later!".
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/components/landing/PsychologistsSection.tsx | grep 'Card'` returns a match.
    - `cat apps/web/src/components/landing/PartnerContentSection.tsx | grep 'Coming soon'` returns a match.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - apps/web/src/components/landing/CTASection.tsx
    - apps/web/src/components/landing/ClinicsLocationsSection.tsx
  </read_first>
  <action>
    Update `CTASection.tsx` with a primary Button "Take the First Step" with `bg-[#2563EB]`. In `ClinicsLocationsSection.tsx`, render the clinic locations (Jakarta & BSD) from the i18n data.
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/components/landing/CTASection.tsx | grep 'Take the First Step'` returns a match.
    - `cat apps/web/src/components/landing/CTASection.tsx | grep 'bg-\[#2563EB\]'` returns a match.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - apps/web/src/pages/LandingPage.tsx
  </read_first>
  <action>
    Update `apps/web/src/pages/LandingPage.tsx` to import and render all 9 section components in strict vertical order: `<HeroSection />`, `<AboutUsSection />`, `<WhyAttentiveSection />`, `<PsychologistsSection />`, `<TestimonialsSection />`, `<ServicesSection />`, `<PartnerContentSection />`, `<CTASection />`, `<ClinicsLocationsSection />`. Remove any old broken imports.
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/pages/LandingPage.tsx | grep '<HeroSection />'` returns a match.
    - `cat apps/web/src/pages/LandingPage.tsx | grep '<ClinicsLocationsSection />'` returns a match.
  </acceptance_criteria>
</task>

## Verification Criteria
- `grep -r "Take the First Step" apps/web/src/components/landing/` returns results in HeroSection and CTASection.
- `ls apps/web/public/images/` exits with 0.
- `grep '"hero"' apps/web/src/locales/en.json` returns a match.
- LandingPage.tsx contains all 9 components imported and rendered.
