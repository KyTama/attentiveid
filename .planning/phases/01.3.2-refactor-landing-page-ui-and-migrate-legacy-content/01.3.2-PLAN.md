---
wave: 1
depends_on: ["Phase 1.3"]
files_modified:
  - "apps/web/public/images/**"
  - "apps/web/src/locales/en.json"
  - "apps/web/src/locales/id.json"
  - "apps/web/components/ui/button.tsx"
  - "apps/web/components/ui/card.tsx"
  - "apps/web/components/ui/avatar.tsx"
  - "apps/web/src/components/landing/HeroSection.tsx"
  - "apps/web/src/components/landing/AboutUsSection.tsx"
  - "apps/web/src/components/landing/WhyAttentiveSection.tsx"
  - "apps/web/src/components/landing/PsychologistsSection.tsx"
  - "apps/web/src/components/landing/TestimonialsSection.tsx"
  - "apps/web/src/components/landing/ServicesSection.tsx"
  - "apps/web/src/components/landing/PartnerContentSection.tsx"
  - "apps/web/src/components/landing/CTASection.tsx"
  - "apps/web/src/components/landing/ClinicsLocationsSection.tsx"
  - "apps/web/src/pages/LandingPage.tsx"
autonomous: true
---

# Phase 01.3.2 Execution Plan
**Goal:** Refactor Landing Page UI to new Figma design and migrate legacy YAML content.

## requirements
- null

## must_haves
- All 9 components must be vertically stacked in the exact order specified.
- Shadcn UI (radix/lucide-react) components used where appropriate.
- i18n JSON files correctly populated from the legacy YAML.
- "Take the First Step" CTA styled with `#2563EB`.

## Wave 1: Data Migration & Setup

<task>
  <read_first>
    - /Users/kytama/Projects/Neo-Attentive/attentiveid.github.io/data/en/homepage.yml
    - apps/web/src/locales/en.json
    - apps/web/src/locales/id.json
  </read_first>
  <action>
    Extract all textual content from the legacy `homepage.yml` file and map it into JSON objects for `apps/web/src/locales/en.json` and `apps/web/src/locales/id.json`. Create the following top-level keys in both JSON files: "hero", "about_us", "why_attentive", "psychologists", "testimonials", "services", "partner_content", "cta", "clinics_locations". For empty legacy data (e.g. missing partner content), set fallback values: title="Coming soon", description="We are currently preparing this section. Check back later!". Both `en.json` and `id.json` should contain identical string values initially (English text from the YAML), so they are structurally ready for translation.
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/locales/en.json | grep '"hero":'` returns a match.
    - `cat apps/web/src/locales/id.json | grep '"cta":'` returns a match.
    - `cat apps/web/src/locales/en.json | grep '"Coming soon"'` returns a match.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - .planning/phases/01.3.2-refactor-landing-page-ui-and-migrate-legacy-content/01.3.2-CONTEXT.md
  </read_first>
  <action>
    Copy the entire legacy images directory from `/Users/kytama/Projects/Neo-Attentive/attentiveid.github.io/images/` to the new monorepo public folder at `apps/web/public/images/`. This can be done via `cp -r /Users/kytama/Projects/Neo-Attentive/attentiveid.github.io/images/* apps/web/public/images/`. Create the destination directory if it does not exist.
  </action>
  <acceptance_criteria>
    - `ls apps/web/public/images/` exits with 0 and outputs file names.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - package.json
  </read_first>
  <action>
    Install required shadcn UI components into the React web app. Execute `npx shadcn@latest add button card avatar` inside the `apps/web` directory to add the Button, Card, and Avatar components. Ensure they use `lucide-react` for icons.
  </action>
  <acceptance_criteria>
    - `ls apps/web/components/ui/button.tsx` exits with 0.
    - `ls apps/web/components/ui/card.tsx` exits with 0.
    - `ls apps/web/components/ui/avatar.tsx` exits with 0.
  </acceptance_criteria>
</task>

## Wave 2: Scaffold Section Components

<task>
  <read_first>
    - .planning/phases/01.3.2-refactor-landing-page-ui-and-migrate-legacy-content/01.3.2-UI-SPEC.md
  </read_first>
  <action>
    Create 9 distinct React component files in `apps/web/src/components/landing/`: `HeroSection.tsx`, `AboutUsSection.tsx`, `WhyAttentiveSection.tsx`, `PsychologistsSection.tsx`, `TestimonialsSection.tsx`, `ServicesSection.tsx`, `PartnerContentSection.tsx`, `CTASection.tsx`, and `ClinicsLocationsSection.tsx`. Each file must export a functional React component that returns a `<section>` tag with an `id` matching its name (e.g., `id="hero-section"`). Import and use the `useTranslation` hook from `react-i18next` in each to prepare for i18n data. Apply font class `font-sans` (Inter).
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/components/landing/HeroSection.tsx | grep 'export function HeroSection'` returns a match.
    - `cat apps/web/src/components/landing/ClinicsLocationsSection.tsx | grep 'id="clinics-locations-section"'` returns a match.
    - `cat apps/web/src/components/landing/HeroSection.tsx | grep 'useTranslation'` returns a match.
  </acceptance_criteria>
</task>

## Wave 3: UI Integration and Styling

<task>
  <read_first>
    - apps/web/src/components/landing/HeroSection.tsx
    - apps/web/src/components/landing/AboutUsSection.tsx
    - apps/web/src/components/landing/WhyAttentiveSection.tsx
    - .planning/phases/01.3.2-refactor-landing-page-ui-and-migrate-legacy-content/01.3.2-UI-SPEC.md
  </read_first>
  <action>
    Update `HeroSection.tsx`, `AboutUsSection.tsx`, and `WhyAttentiveSection.tsx` to display content from the i18n catalogs. In `HeroSection.tsx`, include a Button component from shadcn with text "Take the First Step" and apply the Tailwind class `bg-[#2563EB]` for the Accent color. Set the main background of sections to `bg-[#FFFFFF]` (Dominant) and use `bg-[#F8FAFC]` for inner cards (Secondary). Apply typographic sizes: Display (`text-[48px] font-bold`) for the Hero title, Heading (`text-[24px] font-bold`) for section titles, and Body (`text-[16px] leading-[1.5]`) for paragraphs.
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/components/landing/HeroSection.tsx | grep 'Take the First Step'` returns a match.
    - `cat apps/web/src/components/landing/HeroSection.tsx | grep 'bg-\[#2563EB\]'` returns a match.
    - `cat apps/web/src/components/landing/AboutUsSection.tsx | grep 'bg-\[#FFFFFF\]'` returns a match.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - apps/web/src/components/landing/PsychologistsSection.tsx
    - apps/web/src/components/landing/TestimonialsSection.tsx
    - apps/web/src/components/landing/ServicesSection.tsx
    - apps/web/src/components/landing/PartnerContentSection.tsx
    - .planning/phases/01.3.2-refactor-landing-page-ui-and-migrate-legacy-content/01.3.2-UI-SPEC.md
  </read_first>
  <action>
    Update the next 4 sections to display content from the i18n catalogs. For arrays like psychologists and testimonials, map over the i18n array and use the shadcn `Card` component to render each item. If an i18n value represents an empty state (e.g., Partner Content), ensure the UI renders the explicit fallback copywriting: "Coming soon" and "We are currently preparing this section. Check back later!".
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/components/landing/PsychologistsSection.tsx | grep 'Card'` returns a match.
    - `cat apps/web/src/components/landing/PartnerContentSection.tsx | grep 'Coming soon'` returns a match.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - apps/web/src/components/landing/CTASection.tsx
    - apps/web/src/components/landing/ClinicsLocationsSection.tsx
    - .planning/phases/01.3.2-refactor-landing-page-ui-and-migrate-legacy-content/01.3.2-UI-SPEC.md
  </read_first>
  <action>
    Update `CTASection.tsx` and `ClinicsLocationsSection.tsx`. In `CTASection.tsx`, create a prominent visual anchor section with a primary CTA Button "Take the First Step" using `bg-[#2563EB]`. In `ClinicsLocationsSection.tsx`, render the clinic locations (Jakarta & BSD) from the i18n data.
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/components/landing/CTASection.tsx | grep 'Take the First Step'` returns a match.
    - `cat apps/web/src/components/landing/CTASection.tsx | grep 'bg-\[#2563EB\]'` returns a match.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - apps/web/src/pages/LandingPage.tsx
  </read_first>
  <action>
    Update the main landing page orchestrator (`apps/web/src/pages/LandingPage.tsx`). Import all 9 section components and render them in strict vertical order: `<HeroSection />`, `<AboutUsSection />`, `<WhyAttentiveSection />`, `<PsychologistsSection />`, `<TestimonialsSection />`, `<ServicesSection />`, `<PartnerContentSection />`, `<CTASection />`, `<ClinicsLocationsSection />`. Add scroll-driven reveal animations using Tailwind classes (e.g., `fade-in-up` or intersection observers if necessary) to the wrapper of each section.
  </action>
  <acceptance_criteria>
    - `cat apps/web/src/pages/LandingPage.tsx | grep '<HeroSection />'` returns a match.
    - `cat apps/web/src/pages/LandingPage.tsx | grep '<ClinicsLocationsSection />'` returns a match.
  </acceptance_criteria>
</task>

## Wave 4: Testing

<task>
  <read_first>
    - apps/web/src/components/landing/HeroSection.tsx
    - apps/web/src/components/landing/AboutUsSection.tsx
    - apps/web/src/components/landing/WhyAttentiveSection.tsx
    - apps/web/src/components/landing/PsychologistsSection.tsx
    - apps/web/src/components/landing/TestimonialsSection.tsx
    - apps/web/src/components/landing/ServicesSection.tsx
    - apps/web/src/components/landing/PartnerContentSection.tsx
    - apps/web/src/components/landing/CTASection.tsx
    - apps/web/src/components/landing/ClinicsLocationsSection.tsx
  </read_first>
  <action>
    Create unit tests for all 9 section components to ensure they render correctly and display the fallback/actual content properly. Write the tests in `apps/web/src/components/landing/__tests__/` (e.g. `HeroSection.test.tsx`, `AboutUsSection.test.tsx`, etc.). Verify that the translations load properly or mock the `useTranslation` hook for the tests.
  </action>
  <acceptance_criteria>
    - `ls apps/web/src/components/landing/__tests__/HeroSection.test.tsx` exits with 0.
    - `ls apps/web/src/components/landing/__tests__/ClinicsLocationsSection.test.tsx` exits with 0.
  </acceptance_criteria>
</task>

## Verification Criteria
- `grep -r "Take the First Step" apps/web/src/components/landing/` returns results in HeroSection and CTASection.
- `ls apps/web/public/images/` lists migrated assets.
- Both `apps/web/src/locales/en.json` and `id.json` contain the key `"hero"`.
- LandingPage.tsx contains all 9 components imported and rendered.
- Unit tests exist for the 9 new section components.
