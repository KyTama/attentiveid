# Phase 01.3.1: Landing Page Content Adjustment - Research

## Objective
Answer: "What do I need to know to PLAN this phase well?"

## 1. Components to Implement & Update
The landing page (`apps/web/src/pages/LandingPage.tsx`) needs to follow a strict top-to-bottom rendering order. The components involved are:

1. **`<Hero />`**: Defer image implementation (use placeholders) until final assets arrive.
2. **`<WhyUs />` (New)**: Replaces the old `Services.tsx`. Implements "Kenapa memilih Attentive?" displaying 4 core features.
3. **`<Products />`**: Include clinical services (Adult/Child/Family) under "Informasi Layanan Konsultasi". Add a "Download Catalog" block with a `#` placeholder link at the bottom.
4. **`<Psychologists />`**: Convert from grid to a horizontal carousel matching the "Tim Psikolog Attentive" design. Includes exactly 17 psychologists.
5. **`<Insights />` (New)**: Displays partner articles from Muzz.
6. **`<Testimonials />`**: Retain as is in flow.
7. **`<CtaScreening />` (New)**: Implements "Mulai Langkah Pertama Bersama Attentive" CTA section.
8. **`<Contact />`**: Make clinic locations dynamic (Attentive. Jakarta and Attentive. BSD).

## 2. i18n Data Strategy
Translations and structural content must be managed via `react-i18next` targeting `en.json` and `id.json` in `apps/web/src/i18n/locales/`.
*   **`WhyUs`**: Object storing the 4 feature cards.
*   **`Psychologists`**: Array containing details for the 17 psychologists.
*   **`Insights`**: Data structure for the mental health articles.
*   **`Locations`**: Array for the dynamic clinic locations.

## 3. UI/UX & Design Guidelines
*   **Figma Reference**: Per the `GEMINI.md` rules, you MUST check this exact Figma link for design reference: `https://www.figma.com/design/1nvcVPQadGoCOCn3cC8DCY/Attentive-Landing-Page?node-id=0-1&p=f&t=p7zY0xXhV68lFGb3-0`.
*   **Styling**: Use **Tailwind CSS v4** and **shadcn/ui**. Adhere to the design tokens, layout, and branding provided in Figma.
*   **Performance**: Must be mobile-first and maintain a sub-1.5 second load time.

## 4. Operational & Tooling Considerations (Crucial for Planning)
*   **Plane Auto-Sync**: The `GEMINI.md` specifically mandates using the `plane` MCP tools (`update_issue`, etc.) when creating, starting, or updating any task in this phase. *Note:* `STATE.md` mentions a previous "403 Forbidden" error with Plane API, so auth must be verified.
*   **Code Review Graph**: Before modifying code, utilize `code-review-graph` MCP tools (e.g., `semantic_search_nodes`, `get_impact_radius`) rather than generic `grep` to trace imports and impacts of modifying `LandingPage.tsx` and its children.
*   **Local Environment**: `STATE.md` notes that the local Docker daemon might not be running. If the UI relies on backend queries for testing, keep in mind that local DB verification might be skipped.
*   **Required Skills**: When planning implementation, adhere to `.agent/skills/clean-code` (Concise, direct, solution-focused, no over-engineering) and any `.agent/skills/frontend-design` rules that apply to React/Next.js components.
