---
wave: 1
depends_on: []
files_modified:
  - "apps/web/src/pages/LandingPage.tsx"
  - "apps/web/src/components/landing/Hero.tsx"
  - "apps/web/src/components/landing/AboutUs.tsx"
  - "apps/web/src/components/landing/Services.tsx"
  - "apps/web/src/components/landing/Psychologists.tsx"
  - "apps/web/src/components/landing/Products.tsx"
  - "apps/web/src/components/landing/Testimonials.tsx"
  - "apps/web/src/components/landing/Contact.tsx"
  - "apps/web/src/components/landing/Footer.tsx"
autonomous: true
requirements:
  - REQ-04
  - REQ-06
---

# Plan 01.2-01: Implement Landing Page UI Components

## Goal
Re-create Vex landing page layout using React + Tailwind v4 components, prioritizing premium UX with framer-motion and i18n support.

## must_haves
- [ ] Landing page components correctly display in browser with responsive layout.
- [ ] Tailwind v4 styling applied according to Figma tokens (REQ-04, REQ-06).
- [ ] Framer-motion used for animations and scroll transitions (spring physics).
- [ ] No hardcoded text; react-i18next used for all copy.

## Tasks

<task>
<action>
Ensure required dependencies are installed in the `apps/web` workspace:
- Run `npm install framer-motion react-i18next` within `apps/web`.
- Configure Figma design tokens in the CSS `@theme` configuration (e.g., `--color-accent`, `--color-dominant`, `--color-secondary`).
- Call Plane MCP tool to update issue status to in-progress.
</action>
<read_first>
- apps/web/package.json
</read_first>
<acceptance_criteria>
- `grep -q "framer-motion" apps/web/package.json` exits 0
- `grep -q "react-i18next" apps/web/package.json` exits 0
</acceptance_criteria>
</task>

<task>
<action>
Create `apps/web/src/components/landing/Hero.tsx`.
Implement the Hero component matching the design:
- Import `useTranslation` from `react-i18next` and `motion` from `framer-motion`.
- Import `Button` from `shadcn/ui` (e.g., `import { Button } from "@/components/ui/button"`).
- Add a `<section id="hero" className="relative h-screen w-full flex items-center justify-center overflow-hidden">`.
- Add an `h1` using `<motion.h1 className="text-5xl md:text-7xl font-bold tracking-tight mb-4 font-sans" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} transition={{ type: 'spring', stiffness: 100, damping: 20 }} viewport={{ once: true }}>`.
- Use translation keys: `t('landing.hero.hello')`, `t('landing.hero.weAre')`, `t('landing.hero.brand')`, `t('landing.hero.tagline')`.
- Add the CTA button using the shadcn/ui `Button` component with text `t('landing.hero.cta_book')`. Add class `bg-accent` for the Accent color.
</action>
<read_first>
- .planning/phases/01.2-vex-page-sections-porting-tailwind-v4-customization/01.2-UI-SPEC.md
- .planning/phases/01.2-vex-page-sections-porting-tailwind-v4-customization/01.2-PATTERNS.md
- apps/web/src/components/landing/Hero.tsx
</read_first>
<acceptance_criteria>
- `npx tsc --noEmit` exits 0
- `grep -q "import { Button } from \"@/components/ui/button\"" apps/web/src/components/landing/Hero.tsx` exits 0
- `npm run test -- run` passes (or equivalent structure verification).
</acceptance_criteria>
</task>

<task>
<action>
Create `apps/web/src/components/landing/AboutUs.tsx` and `apps/web/src/components/landing/Services.tsx`.
- Import `useTranslation` and `motion`.
- Set backgrounds using the Dominant color `bg-dominant` and text using Secondary `text-secondary`.
- Utilize `shadcn/ui` primitives (e.g., Cards for services) where applicable.
- Add scroll-triggered animations using `<motion.div whileInView={{ opacity: 1, y: 0 }} initial={{ opacity: 0, y: 20 }}>`.
- Define text using keys like `t('landing.about.title')` and `t('landing.services.title')`.
- Use typography roles from UI-SPEC: headings get `text-4xl font-semibold leading-tight` and body gets `text-base font-normal leading-relaxed`.
- Write unit/integration tests for these components using Vitest and React Testing Library following the AAA pattern.
- Call Plane MCP tool (`update_issue`) to update ticket status after this task.
</action>
<read_first>
- .planning/phases/01.2-vex-page-sections-porting-tailwind-v4-customization/01.2-UI-SPEC.md
- apps/web/src/components/landing/AboutUs.tsx
- apps/web/src/components/landing/Services.tsx
</read_first>
<acceptance_criteria>
- `npx tsc --noEmit` exits 0
- `npm run test -- run` passes
- `grep -q "framer-motion" apps/web/src/components/landing/AboutUs.tsx` exits 0
</acceptance_criteria>
</task>

<task>
<action>
Create the remaining landing page components: `Psychologists.tsx`, `Products.tsx`, `Testimonials.tsx`, `Contact.tsx`, `Footer.tsx` inside `apps/web/src/components/landing/`.
- Apply spacing scale classes (e.g., `gap-4` for md: 16px, `p-6` for lg: 24px, `mb-12` for 2xl: 48px).
- Replace all hardcoded strings with `react-i18next` keys (e.g., `t('landing.contact.title')`).
- Integrate `shadcn/ui` primitives for interactive elements.
- Export each component correctly.
- Write unit tests for each component using Vitest.
- Call Plane MCP tool (`update_issue`) to update ticket status after this task.
</action>
<read_first>
- .planning/phases/01.2-vex-page-sections-porting-tailwind-v4-customization/01.2-UI-SPEC.md
- apps/web/src/components/landing/Contact.tsx
- apps/web/src/components/landing/Footer.tsx
</read_first>
<acceptance_criteria>
- `npx tsc --noEmit` exits 0
- `npm run test -- run` passes
- `grep -q "react-i18next" apps/web/src/components/landing/Contact.tsx` exits 0
</acceptance_criteria>
</task>

<task>
<action>
Update `apps/web/src/pages/LandingPage.tsx` to compose all the new landing components.
- Import `Hero`, `AboutUs`, `Services`, `Psychologists`, `Products`, `Testimonials`, `Contact`, `Footer` from `@/components/landing`.
- Ensure the main wrapper has `<div className="min-h-screen bg-dominant">`.
- Render the components sequentially inside `<main>`.
- Update corresponding integration tests for the composed LandingPage.
- Call Plane MCP tool (`update_issue`) to update ticket status to completed.
</action>
<read_first>
- .planning/phases/01.2-vex-page-sections-porting-tailwind-v4-customization/01.2-PATTERNS.md
- apps/web/src/pages/LandingPage.tsx
</read_first>
<acceptance_criteria>
- `npx tsc --noEmit` exits 0
- `npm run build` succeeds for the web app (verifying composition and type safety).
- `npm run test -- run` passes
</acceptance_criteria>
</task>

<task>
<action>
Create `apps/web/scripts/content-migration.ts` (or similar script) and migrate assets into the project.
- Port all required images, icons, and fonts from Figma/Vex assets into `apps/web/public/assets`.
- Ensure assets are optimized and properly referenced in the components.
- Prepare any necessary JSON files or dictionaries for content that isn't handled by i18next yet.
</action>
<read_first>
- .planning/phases/01.2-vex-page-sections-porting-tailwind-v4-customization/01.2-UI-SPEC.md
</read_first>
<acceptance_criteria>
- Assets are present in `apps/web/public/assets`.
- `npx tsc --noEmit` exits 0.
</acceptance_criteria>
</task>

## Verification
- Code passes `npx tsc --noEmit` without errors.
- Visual check in browser to verify layout aligns with Figma and tailwind v4 styles.
